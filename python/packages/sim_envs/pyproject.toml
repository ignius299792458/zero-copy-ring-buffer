[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
name = "sim_envs"
version = "0.1.0"
description = "Isolated simulation environments package"
authors = ["ignius299792458"]

[tool.poetry.dependencies]
python = ">=3.11,<3.15"
